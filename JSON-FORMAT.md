# RT-Handover JSON 匯入格式說明

> 對應版本：V0.4.0
> 用法：交班單頁面左上「📥 匯入 JSON」→ 選擇 `.json` 檔（UTF-8 編碼）

---

## 完整範例

```json
{
  "交班醫師": "林伯儒 醫師",
  "代理醫師": "劉育昌 醫師",
  "交班期間": { "起": "20260701", "迄": "20260731" },
  "病人": [
    {
      "病歷號碼": "0824566",
      "姓名": "張慶鏞",
      "癌別": "鼻咽癌",
      "預定次數": 25,
      "單次劑量": 180,
      "已完成次數": 11,
      "診斷部位": "cT3N1M0，PTV 含雙側頸部",
      "副作用項目": "口腔黏膜炎",
      "副作用分級": "Gr.2",
      "副作用補充": "體重 -2.5%，已轉介營養師",
      "注意事項": "每週抽血、體重監控",
      "其他備註": ""
    },
    {
      "病歷號碼": "1354100",
      "姓名": "施水環",
      "癌別": "肺癌",
      "預定次數": 10,
      "單次劑量": 300,
      "已完成次數": 5
    }
  ]
}
```

## 最小可用範例

只有「病人」是必填，每位病人所有欄位皆可省略（省略＝留空待手動填寫）：

```json
{ "病人": [ { "姓名": "王小明" }, {} ] }
```

---

## 欄位定義

### 最外層

| 欄位 | 型別 | 必填 | 說明 |
|------|------|:---:|------|
| `交班醫師` | 字串 | 否 | 覆蓋頁面預設值（林伯儒 醫師） |
| `代理醫師` | 字串 | 否 | 覆蓋頁面預設值（劉育昌 醫師） |
| `交班期間` | 物件 `{起, 迄}` | 否 | 西元日期，接受 `"20260701"`、`"2026/07/01"`、`"2026-07-01"`，匯入後一律顯示為 `2026/07/01` |
| `病人` | 陣列 | **是** | 病人清單，可為空陣列 |

### 病人（陣列中每個物件）

| 欄位 | 型別 | 必填 | 說明 |
|------|------|:---:|------|
| `病歷號碼` | 字串 | 否 | 建議用字串保留前導零（`"0824566"`） |
| `姓名` | 字串 | 否 | |
| `癌別` | 字串 | 否 | 建議用下方清單值；清單外的值會**動態加入選單**，不會失真 |
| `預定次數` | 數字 | 否 | 正整數 |
| `單次劑量` | 數字 | 否 | cGy，正整數 |
| `已完成次數` | 數字 | 否 | 0 或正整數；**剩餘次數與累計劑量由系統自動計算，不要提供** |
| `診斷部位` | 字串 | 否 | 自由文字 |
| `副作用項目` | 字串 | 否 | 建議用下方清單值；清單外同癌別規則 |
| `副作用分級` | 字串 | 否 | `"Gr.1"` ~ `"Gr.4"` |
| `副作用補充` | 字串 | 否 | 自由文字 |
| `注意事項` | 字串 | 否 | 自由文字 |
| `其他備註` | 字串 | 否 | 自由文字 |

### 癌別建議值

`頭頸癌`｜`鼻咽癌`｜`口腔癌`｜`食道癌`｜`肺癌`｜`乳癌`｜`肝癌`｜`胃癌`｜`胰臟癌`｜`大腸直腸癌`｜`攝護腺癌`｜`子宮頸癌`｜`子宮內膜癌`｜`卵巢癌`｜`膀胱癌`｜`腦瘤`｜`淋巴癌`｜`皮膚癌`｜`骨轉移`｜`腦轉移`｜`其他`

### 副作用項目建議值

`無明顯副作用`｜`放射性皮膚炎`｜`口腔黏膜炎`｜`放射性食道炎`｜`吞嚥疼痛／困難`｜`噁心嘔吐`｜`腹瀉`｜`放射性膀胱炎`｜`放射性直腸炎`｜`放射性肺炎`｜`疲倦`｜`體重下降`｜`骨髓抑制`｜`其他`

---

## 匯入行為

1. 頁面**已有病人**時會詢問：「確定」＝清空後匯入；「取消」＝附加在現有名單後
2. 交班醫師／代理醫師／交班期間只在 JSON 有提供時才覆蓋
3. 數值欄位無效值（非數字）→ 該欄留空，卡片顯示「資料未完整」
4. 格式錯誤（非合法 JSON、缺「病人」陣列）→ 顯示錯誤訊息，不影響現有資料

## 注意

- 檔案請存成 **UTF-8** 編碼（Excel 另存 JSON 或程式輸出時留意）
- 本檔案含病人個資時，比照鎖定版原則：**不要放進 Git repo、不要上傳雲端公開空間**
