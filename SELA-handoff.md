# SELA-handoff.md — RT-Handover V0.3.0（更新）

> 產出理由（鐵律 #0）：累積 ≥ 3 條專案坑（P1/P2/P3）。
> 產出時間：2026-07，V0.2.0 首發；V0.3.0 更新（P1 deprecated、新增 P4）。

---

## 〇、專案速覽

- **專案：** RT-Handover（彰濱秀傳放射腫瘤科 病人交班單）
- **技術棧：** 純靜態單頁 HTML + Vanilla JS，離線可用
- **定位：** 醫師休假前的病人交班工具，核心機制是「另存已填寫版本」——把使用者填寫後的頁面整份序列化成新 HTML 檔，可寄給代理醫師直接開
- **規模：** 單檔 ~500 行，V0.1.0 → V0.2.0 共兩版

## 一、用 Kit 的整體感受

Kit 對「純靜態小工具」路徑覆蓋良好（reference-static-pages、坑 #3/#26/#27 種子直接可用）。配色決策流程順：V0.1.0 依醫療預設提北歐霧藍，V0.2.0 SELA 主動指定 `#48541D`，符合「決策權在 SELA」的鐵律，無摩擦。

## 二、發現的「跨專案通用坑」（建議進 Kit）

**候選一條（三坑收斂為一個模式）：「自包含可另存 HTML」序列化三件套**

症狀：做「把使用者填寫內容連頁面一起另存成新 HTML」的工具時，三個地方會各自靜默失效——
1. 重開後 script 重跑初始建構、洗掉已填內容（P1）
2. `<input>` 即時值只在 property、不進 `outerHTML`（P2）
3. `<select>` 選擇同理，`selected` attribute 不會自動更新（P3）

做法（三件套，缺一不可）：
- `<body data-saved="1">` guard，script 據此跳過初始建構
- 序列化前 `input.setAttribute("value", input.value)` 逐一寫回
- `option.toggleAttribute("selected", ...)` 逐一寫回（change 事件即時寫 + 存檔前保險再寫）
- 配套：所有互動事件用**委派**綁容器，重開的 DOM 免重綁

通用性評估：任何「單檔可攜、免後端、使用者填寫後要帶著走」的工具都會踩（表單、檢核表、報告產生器）。與既有坑 #3（資料 vs 顯示）相鄰但不重疊——#3 講讀取真相，這條講**序列化／clone 時 property → attribute 的落差**。checkbox/radio 的 `checked` 屬性可預期是同族第四坑，遇到時併入。

**V0.3.0 更新——收斂重寫這條提案：**
- P1（script 重跑洗內容）被架構性解決：另存版改為**無 script 唯讀快照**（clone → 轉純文字 → 拔互動）。教訓升級為設計模式：「另存版若不需要再編輯，直接產無 script 靜態快照，比 guard 更穩」。
- 新增 P4：**單檔快照必須自包含**——相對路徑資源（favicon、圖）離開資料夾即斷鏈，產檔時要 inline 或移除。
- 提案文字建議改為兩段式：(a) property→attribute 落差（P2/P3，clone 與 outerHTML 都踩）；(b) 單檔快照自包含原則（P4）。P1 降為 (a) 的注腳（僅「可再編輯另存版」需要 guard）。

## 三、發現的「跨專案設計模式」

（無獨立新模式——「事件委派容器」已是常識級，不提案）

## 四、Kit 該瘦身或調整的地方

（無）

## 五、留在這個專案、不要回流 Kit 的東西

- 病人資料結構（mrn/total/dose/done）、癌別清單、剩餘次數 ≤5 警示門檻——放腫業務邏輯
- 「即將完成＝交班期間需結案」的領域判斷
- 民國日期格式慣例

## 六、Kit Claude 的建議行動清單

1. 評估第二節「序列化三件套」是否進 `cross-project-pitfalls.md`（建議 F 或 K 章，鄰 #3）；若認定 N=1 可先記候補，但此模式在「可攜單檔工具」類重現機率高
2. 其餘無

## 七、最後備註

本專案含病人個資，部署限 private repo——若 Kit 未來加「個資專案部署檢查」條目，此案可作範例。
